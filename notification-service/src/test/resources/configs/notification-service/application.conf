service.name = anomaly-event-processor
service.admin.port = 8099

main.class = org.hypertrace.alert.notification.service.NotificationEventProcessorService

logger.names = ["file"]
logger.file.dir = "/var/logs/anomaly-event-processor"

rules.path = "build/resources/test/configs/rules.json"

queue.config {
  type = "kafka"
  kafka = {
    topic = "alert-tasks"
    bootstrap.servers = "localhost:9092"
    consumer = {

    }
  }
}