apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}
  labels:
    release: {{ .Release.Name }}
  {{- with .Values.metricAnomalyDetectorConfig.deploymentLabels }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.metricAnomalyDetectorConfig.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: {{ .Values.metricAnomalyDetectorConfig.maxUnavailable }}
  selector:
    matchLabels:
      {{- toYaml .Values.metricAnomalyDetectorConfig.deploymentSelectorMatchLabels | nindent 6 }}
  template:
    metadata:
      labels:
        release: {{ .Release.Name }}
      {{- with .Values.metricAnomalyDetectorConfig.podLabels }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/metric-anomaly-detector/application-config.yaml") . | sha256sum }}
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.metricAnomalyDetectorConfig.containerAdminPort | quote }}
      {{- with .Values.metricAnomalyDetectorConfig.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
    {{- with .Values.metricAnomalyDetectorConfig.imagePullSecrets }}
      imagePullSecrets:
      {{- toYaml . | nindent 8 }}
    {{- end }}
      volumes:
        - name: service-config
          configMap:
            name: {{ .Values.metricAnomalyDetectorConfig.name }}
        - name: log4j-config
          configMap:
            name: {{ .Values.metricAnomalyDetectorConfig.logConfig.name }}
    {{- with .Values.metricAnomalyDetectorConfig.nodeLabels }}
      nodeSelector:
      {{- toYaml . | nindent 8}}
    {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ include "metricanomalydetector.image" . }}"
          imagePullPolicy: {{ .Values.metricAnomalyDetectorConfig.image.pullPolicy }}
          ports:
            - name: admin-port
              containerPort: {{ .Values.metricAnomalyDetectorConfig.containerAdminPort }}
              protocol: TCP
          env:
            - name: SERVICE_NAME
              value: "{{ .Chart.Name }}"
            - name: BOOTSTRAP_CONFIG_URI
              value: "file:///app/resources/configs"
            - name: LOG4J_CONFIGURATION_FILE
              value: "/var/{{ .Chart.Name }}/log/log4j2.properties"
            - name: JAVA_TOOL_OPTIONS
              value: {{ .Values.metricAnomalyDetectorConfig.javaOpts | quote }}
          volumeMounts:
            - name: service-config
              mountPath: /app/resources/configs/{{ .Chart.Name }}/application.conf
              subPath: application.conf
            - name: log4j-config
              mountPath: /var/{{ .Chart.Name }}/log
          livenessProbe:
            initialDelaySeconds: {{ int .Values.metricAnomalyDetectorConfig.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ int .Values.metricAnomalyDetectorConfig.livenessProbe.periodSeconds }}
            tcpSocket:
              port: admin-port
          readinessProbe:
            initialDelaySeconds: {{ int .Values.metricAnomalyDetectorConfig.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ int .Values.metricAnomalyDetectorConfig.readinessProbe.periodSeconds }}
            failureThreshold: {{ int .Values.metricAnomalyDetectorConfig.readinessProbe.failureThreshold }}
            httpGet:
              path: /health
              port: {{ .Values.metricAnomalyDetectorConfig.containerAdminPort }}
          resources:
            {{- toYaml .Values.metricAnomalyDetectorConfig.resources | nindent 12 }}