apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.notificationServiceConfig.notificationChannelsConfig.name }}
  labels:
    release: {{ .Release.Name }}
data:
  notification-channels.json: |-
    {{ .Values.notificationServiceConfig.notificationChannelsConfig.channels |  toJson  }}