apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.alertRulesConfig.name }}
  labels:
    release: {{ .Release.Name }}
data:
  alert-rules.json: |-
    {{ .Values.alertRulesConfig.rules |  toJson  }}